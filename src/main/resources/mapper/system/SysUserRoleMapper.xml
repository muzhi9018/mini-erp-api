<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.muzhi.minierp.mapper.system.SysUserRoleMapper">


    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="com.muzhi.minierp.entity.system.SysUserRole">
        <id column="id" property="id" />
        <result column="gmt_create" property="gmtCreate" />
        <result column="gmt_modified" property="gmtModified" />
        <result column="status" property="status" />
        <result column="sort" property="sort" />
        <result column="role_id" property="roleId" />
        <result column="user_id" property="userId" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="base_column_list">
        id,
        gmt_create,
        gmt_modified,
        status,
        sort,
        role_id,
        user_id
    </sql>



    <select id="findRoleCodesByUserId" resultType="java.lang.String">
        select
            role_code
        from sys_user_role sur
        join sys_role sr on sr.id = sur.role_id
        where sur.user_id = #{userId}
    </select>


    <select id="countByRoleId" resultType="java.lang.Long">
        select
            count(sur.id) as  total
        from sys_user_role sur
        where sur.role_id = #{roleId}
    </select>
</mapper>
